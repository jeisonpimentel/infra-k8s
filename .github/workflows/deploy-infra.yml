name: Deploy MySQL & PostgreSQL to GKE

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
      - name: 🛎 Checkout repository
        uses: actions/checkout@v3

      - name: 🔐 Authenticate with Google Cloud
        id: auth
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: 📦 Set up gcloud CLI
        uses: google-github-actions/setup-gcloud@v1

      - name: 🔄 Install GKE Auth Plugin
        run: |
          gcloud components install gke-gcloud-auth-plugin
          echo "export USE_GKE_GCLOUD_AUTH_PLUGIN=True" >> $GITHUB_ENV

      - name: ⚙️ Configure kubectl
        run: |
          gcloud container clusters get-credentials cluster-gke-standard --region=us-west1
          kubectl cluster-info

      - name: 🚀 Deploy MySQL & PostgreSQL
        run: |
          kubectl apply -f k8s-config/configmaps/configmap.yaml
          kubectl apply -f k8s-config/secrets/secret.yaml
          kubectl apply -f k8s-config/volumes/pv/mysql-pv.yaml
          kubectl apply -f k8s-config/volumes/pvc/mysql-pvc.yaml
          kubectl apply -f k8s-config/volumes/pv/postgres-pv.yaml
          kubectl apply -f k8s-config/volumes/pvc/postgres-pvc.yaml

          kubectl apply -f k8s-config/deployments/deployment-mysql.yaml
          kubectl apply -f k8s-config/deployments/deployment-postgres.yaml
          kubectl apply -f k8s-config/services/svc-mysql.yaml
          kubectl apply -f k8s-config/services/svc-postgres.yaml
          
          kubectl apply -f k8s-config/rbac/

          kubectl rollout status deployment/mysql8
          kubectl rollout status deployment/postgres14